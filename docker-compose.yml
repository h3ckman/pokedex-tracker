services:
  postgres:
    image: postgres:18-alpine
    ports:
      - "5436:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pokedex-tracker
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
